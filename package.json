{
  "name": "kls-sameday-office",
  "private": true,
  "version": "26.37.0",
  "scripts": {
    "test": "node --check app.js && node --check driver.js && node --check sw.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8'));JSON.parse(require('fs').readFileSync('vercel.json','utf8'));console.log('KLS v26.37 checks passed')\""
  }
}
